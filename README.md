# assignment-1-mobile



This app is called Hybrid/Shell architecture because the native app only works as a container that shows a website using WebView.
The interface is not made with native React Native components.
Instead, it loads a web page from the internet (Google).
